---
title: help-macos.txt — macOS platform notes (pointer spec)
summary: Pointer spec: one anchor per section of assets/help-macos.txt; the help text is the normative contract.
---

# help-macos.txt — macOS platform notes (pointer spec)

This is a pointer spec. `assets/help-macos.txt` is the single source of
truth for this surface; each section below carries a stable anchor and
points at its normative text. No behavior prose is duplicated here.

## WHEN YOU NEED THIS {#260908-macos-when-you-need-this}
See `assets/help-macos.txt` section `== WHEN YOU NEED THIS ==`.

## CHECK {#260908-macos-check}
See `assets/help-macos.txt` section `== CHECK ==`.

## LOCAL BUILD AND RUN {#260908-macos-local-build-and-run}
See `assets/help-macos.txt` section `== LOCAL BUILD AND RUN ==`.

## WHO OWNS THE PERMISSION (RESPONSIBLE PROCESS) {#260908-macos-who-owns-the-permission-responsible-process}
See `assets/help-macos.txt` section `== WHO OWNS THE PERMISSION (RESPONSIBLE PROCESS) ==`.

## STABLE SIGNING IDENTITY (SURVIVING REBUILDS) {#260908-macos-stable-signing-identity-surviving-rebuilds}
See `assets/help-macos.txt` section `== STABLE SIGNING IDENTITY (SURVIVING REBUILDS) ==`.

## GRANTING PERMISSIONS: PROCEDURE {#260908-macos-granting-permissions-procedure}
See `assets/help-macos.txt` section `== GRANTING PERMISSIONS: PROCEDURE ==`.

## SESSION BRIDGE FOR SSH (REMOTE MAC) {#260908-macos-session-bridge-for-ssh-remote-mac}
See `assets/help-macos.txt` section `== SESSION BRIDGE FOR SSH (REMOTE MAC) ==`.

## SESSION, LOCK, SECURE INPUT {#260908-macos-session-lock-secure-input}
See `assets/help-macos.txt` section `== SESSION, LOCK, SECURE INPUT ==`.

## DISPLAYS AND COORDINATES {#260908-macos-displays-and-coordinates}
See `assets/help-macos.txt` section `== DISPLAYS AND COORDINATES ==`.

## CAVEATS {#260908-macos-caveats}
See `assets/help-macos.txt` section `== CAVEATS ==`.
