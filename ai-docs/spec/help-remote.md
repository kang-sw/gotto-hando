---
title: help-remote.txt — remote (ssh) destinations (pointer spec)
summary: Pointer spec: one anchor per section of assets/help-remote.txt; the help text is the normative contract.
---

# help-remote.txt — remote (ssh) destinations (pointer spec)

This is a pointer spec. `assets/help-remote.txt` is the single source of
truth for this surface; each section below carries a stable anchor and
points at its normative text. No behavior prose is duplicated here.

## WHEN YOU NEED THIS {#260908-remote-when-you-need-this}
See `assets/help-remote.txt` section `== WHEN YOU NEED THIS ==`.

## SECURITY MODEL (READ FIRST) {#260908-remote-security-model-read-first}
See `assets/help-remote.txt` section `== SECURITY MODEL (READ FIRST) ==`.

## HOW IT WORKS {#260908-remote-how-it-works}
See `assets/help-remote.txt` section `== HOW IT WORKS ==`.

## SESSION BRIDGE {#260908-remote-session-bridge}
See `assets/help-remote.txt` section `== SESSION BRIDGE ==`.

## REMOTE SETUP: COMMON {#260908-remote-remote-setup-common}
See `assets/help-remote.txt` section `== REMOTE SETUP: COMMON ==`.

## REMOTE MAC (OVER SSH) {#260908-remote-remote-mac-over-ssh}
See `assets/help-remote.txt` section `== REMOTE MAC (OVER SSH) ==`.

## REMOTE WINDOWS (BRIDGE) {#260908-remote-remote-windows-bridge}
See `assets/help-remote.txt` section `== REMOTE WINDOWS (BRIDGE) ==`.

## QUOTING {#260908-remote-quoting}
See `assets/help-remote.txt` section `== QUOTING ==`.

## TROUBLESHOOTING {#260908-remote-troubleshooting}
See `assets/help-remote.txt` section `== TROUBLESHOOTING ==`.
